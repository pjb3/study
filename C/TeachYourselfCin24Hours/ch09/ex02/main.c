#include <stdio.h>

int main (int argc, const char * argv[]) {
  printf("short int %d\n", sizeof(short int));
  printf("long int %d\n", sizeof(long int));
  printf("long double %d\n", sizeof(long double));
  return 0;
}
