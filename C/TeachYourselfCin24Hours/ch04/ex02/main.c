#include <stdio.h>

int main (int argc, const char * argv[]) {
  char x = 72;
  char y = 104;
  
  printf("The character value of %d is %c\n", x, x);
  printf("The character value of %d is %c\n", y, y);
  return 0;
}
